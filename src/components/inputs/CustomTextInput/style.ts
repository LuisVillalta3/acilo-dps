import { StyleSheet } from "react-native";

export default StyleSheet.create({
  outlinedStyle: {
    backgroundColor: 'white',
    borderColor: "#808080"
  },
  activeOutlineColor: {
    borderColor: "#27B2B3"
  }
})